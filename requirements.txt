# Direct dependencies, version-pinned. For supply-chain-hardened (hash-pinned,
# tamper-evident) installs, generate requirements.lock via scripts/lock-deps.sh
# and commit it — the Docker image then installs with --require-hashes from it.
#
# --- Core ---
aiogram==3.30.0
fastapi==0.139.2
uvicorn[standard]==0.51.0
gunicorn==23.0.0
pydantic==2.13.4
pydantic-settings==2.14.2

# --- DB / cache / queue ---
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.5
redis==5.2.1
arq==0.28.0
# FIX: AUDIT-17 - moved to requirements-dev.txt

# --- Object storage ---
aioboto3==13.3.0

# --- AI providers ---
openai==1.59.6
anthropic==0.117.0
# google-genai: moved off the pre-1.0 0.3.0 (unstable API) to a stable release.
# The adapter already uses the stable unified API (genai.Client + aio.models.
# generate_content), so this is API-compatible — but smoke-test the Gemini path
# after upgrading.
google-genai==2.13.0
httpx==0.28.1

# --- Payments ---
yookassa==3.11.0
stripe==11.4.1

# --- Image upload validation (Mini App effects) ---
pillow==12.3.0

# --- Document extraction (Premium docs) ---
pypdf==6.14.2
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2

# --- Utilities ---
structlog==24.4.0
sentry-sdk==2.66.0
fluent.runtime==0.4.0
python-multipart==0.0.32
argon2-cffi==25.1.0
pyotp==2.10.0
pyjwt==2.13.0
cryptography==48.0.1
